name: Keep Supabase Active

on:
  schedule:
    # 每天 UTC 0 点运行
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping:
        runs-on: ubuntu-latest
        steps:
          - name: Ping Waline Comment API
            run: |
              curl -i -X GET "https://waline.myvnc.com/api/comment?path=%2F&lang=zh-CN"
